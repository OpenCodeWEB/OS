{
  "name": "opencodewebsag-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — webhook handler for OpenCodeWEBsAG autonomous bot",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
